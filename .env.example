# Optional analytics identifier
NEXT_PUBLIC_GA_ID=

# Firebase Web SDK (public identifiers, configured only for Vercel Production)
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_APPCHECK_SITE_KEY=

# Firebase Admin SDK (server-only secrets, configured only for Vercel Production)
FIREBASE_PROJECT_ID=
FIREBASE_WEB_APP_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
COMMUNITY_HASH_SALT=

# Local development uses the demo Firebase Emulator Suite configuration.
# Do not copy production Firebase values or App Check debug tokens locally.
